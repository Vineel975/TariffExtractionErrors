-- 1. What insurer-related columns does Claims actually have?
SELECT COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'Claims'
  AND (COLUMN_NAME LIKE '%Issue%' OR COLUMN_NAME LIKE '%Insur%' OR COLUMN_NAME LIKE '%Payer%')
ORDER BY COLUMN_NAME;
